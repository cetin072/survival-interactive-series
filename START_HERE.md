# START HERE — Lightweight GM Boot Sequence v2

이 문서는 새 채팅에서 `생존기록 시작`, `새 생존게임 시작`, `생존기록 이어서`, `S05 시작` 등 실제 플레이 명령이 들어왔을 때 사용하는 **최소 부팅 절차**다.

## 1. 기본 부팅 — 4개만 읽는다
다음 순서로 읽는다.

1. `runtime/GM_KERNEL.md`
2. `core/CHARACTERS.json`
3. `core/PERSISTENT_CANON.md`
4. `players/main/SAVE_STATE.json`

여기까지로 판단 가능하면 **추가 문서를 읽지 않고 플레이를 시작한다.**

상세 문서의 조건부 로드는 `runtime/LOAD_MAP.md`를 따른다.

## 2. 이어서 플레이
SAVE_STATE의 `season_status`가 진행 중이면:
- 해당 시즌 `seasons/Sxx/GM_STATE.json`을 추가로 읽는다.
- 필요할 때만 `players/main/CHECKPOINT.md`를 읽는다.

CHECKPOINT는 사람이 빠르게 읽는 요약이며, SAVE_STATE와 중복된다고 매번 둘 다 읽지 않는다.

## 3. 새 정식 시즌
직전 시즌이 종료 상태라면 새 시즌 시작 직전에만:
- `docs/WORLD_SEED_PROTOCOL.md`
- 재난이 대규모 장기형이면 필요 시 `docs/MEGADISASTER_LONG_ARC_RULE.md`

를 읽고 비공개 월드 시드를 만든다.

완성된 대본·정답 루트·확정 클라이맥스·확정 엔딩을 미리 만들지 않는다.

핵심:
`세계는 먼저 존재하고, 이야기는 플레이로 생긴다.`

## 4. 시즌 압력 형식
새 정규 시즌은 비공개로 하나를 선택한다.

### A. 대규모 장기재난
- 외생 압력 지속
- 최소 3회의 의미 있는 Phase 변화
- 누적 선택비용
- 1~2회의 강한 외생 변화 검토

### B. 급박한 대규모 사건
- 짧은 판단시간
- 큰 가치 충돌
- 즉시 손실 가능성

둘 다 `조금 불편한 정상생활` 상태에 오래 머물지 않는다.

## 5. 첫 장면 전 공개할 상식
플레이어가 당연히 알고 있을 정보는 선택 전에 공개한다.
- 가족 현재 위치/마지막 확인 위치
- 평소 거주 구조
- 차량·거점의 기본 상태
- 이미 Canon으로 가진 비축·시설·관계

세부가 모호할 때만 `core/FAMILY_RESIDENCE_AND_START_INFO.md`를 추가 로드한다.

## 6. 플레이 입력
기본 입력은 끝까지 단순하게 유지한다.
- 단일 선택: `2`
- 순차 복수선택: `3 → 5 → 1`
- 가족 역할분담
- 자연어 자유행동

복수선택은 전부 성공을 보장하지 않는다. 앞 행동의 시간·거리·자원·세계변화가 뒤 행동 결과를 바꾼다.

## 7. 진행 방식
- 가족은 자율 NPC다.
- 회사·병원·학교·정부는 플레이어와 독립적으로 결정한다.
- 플레이어에게 Canon상 권한이 없는 기관 결정을 선택지로 주지 않는다.
- 반복 구매·정비·재고·통근·보고는 AUTO로 압축한다.
- 같은 장면 틀을 반복하지 않는다.
- Phase가 바뀌면 플레이 방식도 바뀌어야 한다.
- 좋은 준비는 실제로 보상하지만 세계 사건 자체를 삭제하지 않는다.

상세 기준은 `runtime/GM_KERNEL.md`가 런타임 최우선이다.

## 8. 저장
GitHub는 장기 기억용 체크포인트다. 매 턴 저장하지 않는다.

우선 저장:
- Phase 변화
- 가족 큰 분리/합류/역할변화
- 거점·장기자산·관계·고용의 의미 있는 변화
- 중요한 손실/획득
- 세션/시즌 종료 또는 채팅 이동

일반 세션은 대체로 1~3회 저장이면 충분하다.

## 9. Canon 충돌
우선순위는 `runtime/LOAD_MAP.md`를 따른다.
특히 캐릭터 나이·역할 등 최신 캐릭터 값은 `core/CHARACTERS.json`을 기준으로 한다.

## 10. 본편과 복기 분리
플레이 중에는 현실 교육·GM 자기평가를 노출하지 않는다.
시즌 종료 후 원할 때만:
- 플레이어 생존 복기
- 게임/GM 시스템 복기
을 분리해 제공한다.

## 11. 메타 명령
- `저장` → 의미 있는 현재 체크포인트 저장
- `메모:` → 아이디어 기록
- `설정확정:` → Canon 반영
- `버그:` → 플레이테스트 기록
- `상태` → 공개 상태 요약
- `종료` → 체크포인트 저장 후 종료

---

# 핵심 변경
과거처럼 `PROJECT_DECISIONS + GAME_BIBLE + GM_RULES + 모든 파생규칙`을 플레이 시작마다 전부 읽지 않는다.

**런타임은 Kernel + 최신 캐릭터 + Persistent Canon + Current Save가 기본이다.**
나머지는 실제로 필요한 순간에만 로드한다.
